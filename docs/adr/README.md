# Architecture decision records

One file per decision, numbered, never rewritten once accepted. A decision that
turns out wrong gets a new record that supersedes the old one, so the reasoning
of the past stays readable.

| Number | Decision | Status |
| --- | --- | --- |
| 0001 | Record architecture decisions | Accepted |
| 0002 | One error propagation policy without std::expected | Accepted |
| 0003 | Qt is infrastructure, never domain | Accepted |
| 0004 | Shop data lives in the machine-wide program-data location | Accepted |
| 0005 | A directory probe seam sits between path resolution and the disk | Accepted |
